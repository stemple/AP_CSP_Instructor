let PrimeListApp = {

    generatePrimeList: function() {
        // Here is where you need to create the array of prime numbers
        let ub = document.getElementById("upper_bound_input").value;
        console.log(ub);

    },

    renderList: function() {
        // Here is where you need to create the li items and append them to the DOM.

    }

}